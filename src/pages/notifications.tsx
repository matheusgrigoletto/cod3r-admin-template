import type { NextPage } from "next";

import Layout from "~components/Layout/Layout";

const SettingsPage: NextPage = () => {
  return (
    <Layout title="Notifications" subtitle="Notifications page">
      <p>Lorem ipsum</p>
    </Layout>
  );
};

export default SettingsPage;
