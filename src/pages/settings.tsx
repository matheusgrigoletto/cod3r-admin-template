import type { NextPage } from "next";

import Layout from "~components/Layout/Layout";

const SettingsPage: NextPage = () => {
  return <Layout title="Settings" subtitle="Settings page"></Layout>;
};

export default SettingsPage;
